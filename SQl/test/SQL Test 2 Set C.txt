SQL Test 2 Set C


1. Write a SQL statement to display all the information of all actors 
select * from actor;

2. Write a query to display the columns in descending order of rating of movie 
select * from movie order by imdb_rating desc;

3. Write a SQL statement to display names of actors who are male and received awards more  than 10. 
select * from actor where gender="male" and no_of_awards>10;

4. Write a SQL query to display the role followed by all movie id for the actor who is having  actor id as 101 
select role,m_id from movie_detail where a_id=101;	

5. Write a SQL query to find the actor who is not Indian 
select * from actor where nationality !="Indian";

6.6. Write a SQL statement to find the number of actors worked in each movie. 
select m_id , count(a_id) from movie_detail group by m_mid;

Write a SQL statement to find the role played by ‘Amir khan’ actor in 3 idiots movie. 

8.Write a SQL statement to find the details of movie having lowest total budget
select * from movie where t_budget =(select min(t_budget) from movie);

9.Write a SQL statement that finds out count of actors according to gender 
select gender , count(*) from movie_actor group by gender;

10Write a query to find all the roles of actor who have played in movie with id 3.
select role from movie_detail where m_id=103;